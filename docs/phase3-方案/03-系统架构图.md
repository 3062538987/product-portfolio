# 3.3 系统架构图

> 所属阶段：阶段 3 — 方案设计
> 日期：2026-08-04
> 状态：讨论中
> 说明：Plain text 版本，后期在 Figma 中精修为正式图

---

## 一、整体架构（三层模型）

```
┌─────────────────────────────────────────────────────┐
│                    展示层 (Presentation)              │
│                                                     │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │ Navbar  │ │  Hero    │ │ Projects │ │  About  │ │
│  │         │ │ Section  │ │ Section  │ │ Section │ │
│  └─────────┘ └──────────┘ └──────────┘ └─────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ Articles │ │ Contact  │ │  Footer  │            │
│  │ Section  │ │ Section  │ │          │            │
│  └──────────┘ └──────────┘ └──────────┘            │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │  PDFResume (隐藏组件，react-to-print 渲染)    │   │
│  └─────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────┤
│                    数据层 (Data)                     │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │ profile  │  │ projects │  │ articles │         │
│  │  .ts     │  │  .ts     │  │  .ts     │         │
│  └──────────┘  └──────────┘  └──────────┘         │
│       src/data/                                     │
├─────────────────────────────────────────────────────┤
│                    基础设施层 (Infra)                 │
│                                                     │
│  ┌──────┐  ┌──────────┐  ┌──────────┐             │
│  │ Vite │  │ Tailwind │  │ TypeScript│            │
│  │  5.x │  │  CSS 3.x │  │    5.x    │            │
│  └──────┘  └──────────┘  └──────────┘             │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐     │
│  │ lucide-  │  │ react-to │  │  gh-pages    │     │
│  │ react    │  │ -print   │  │  (部署)      │     │
│  └──────────┘  └──────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────┘
```

---

## 二、组件树（详细版）

```
App.tsx
│
├── Navbar
│   ├── Logo (姓名，点击回顶部)
│   ├── NavLinks
│   │   ├── NavLink ("项目作品" → #projects)
│   │   ├── NavLink ("关于我"   → #about)
│   │   └── NavLink ("文章"     → #articles)    [条件渲染: articles.length > 0]
│   ├── MobileMenuToggle (汉堡按钮，< 768px 显示)
│   ├── MobileMenu (全屏下拉菜单，< 768px 显示)
│   └── DownloadPDFButton (触发 react-to-print)
│
├── <main>
│   ├── HeroSection
│   │   ├── Name (h1, 大字)
│   │   ├── Title (h2, "B端产品经理")
│   │   ├── Tagline (p, 一句话定位)
│   │   └── CTAScrollButton ("查看作品 ↓")
│   │
│   ├── ProjectsSection
│   │   ├── SectionTitle ("项目作品集")
│   │   └── ProjectCard[] (×2-3, 手风琴模式)
│   │       ├── ProjectHeader
│   │       │   ├── ProjectName (h3)
│   │       │   ├── ProjectSummary (p, 一句话概述)
│   │       │   ├── KeyMetrics (3个数字, 大字加粗)
│   │       │   └── ExpandToggle (展开/收起按钮)
│   │       └── ProjectDetail (展开态)
│   │           ├── Background (背景与挑战)
│   │           ├── MyRole (我的角色)
│   │           ├── Solution (方案与决策)
│   │           ├── MetricsComparison (Before/After 图表)
│   │           │   └── PowerBIChart (静态图片)
│   │           └── Reflection (复盘与反思)
│   │
│   ├── AboutSection
│   │   ├── SectionTitle ("关于我")
│   │   ├── Bio (p, 2-3句话个人陈述)
│   │   └── CareerGoal (p, 一句话职业目标)
│   │
│   ├── ArticlesSection [条件渲染: articles.length > 0]
│   │   ├── SectionTitle ("精选文章")
│   │   └── ArticleCard[] (×0-2)
│   │       ├── ArticleTitle (h4)
│   │       ├── ArticleDate (span)
│   │       ├── ArticleSummary (p)
│   │       └── ArticleLink (a, target="_blank")
│   │
│   ├── ContactSection
│   │   ├── SectionTitle ("联系方式")
│   │   ├── Email (a, mailto:)
│   │   └── LinkedIn (a, target="_blank")
│   │
│   └── Footer
│       └── Copyright (p)
│
└── PDFResume (display: none, 仅 react-to-print 渲染)
    ├── 个人信息 (A4 排版)
    ├── 项目摘要 (精简版)
    └── 联系方式
```

---

## 三、自定义 Hooks

```
useScrollSpy
├── 输入: sectionIds (string[])
├── 输出: activeSection (当前可见区域 ID)
├── 实现: IntersectionObserver
└── 用途: 导航栏高亮当前所在区域

useAccordion
├── 输入: items (项目卡片数组)
├── 输出: expandedIndex (当前展开的卡片索引), toggle(index)
├── 实现: useState
└── 用途: 手风琴模式，同一时间只展开一个项目卡片
```

---

## 四、数据模型

### profile.ts
```typescript
interface Profile {
  name: string;           // 姓名
  title: string;          // 岗位，如 "B端产品经理"
  tagline: string;        // 一句话定位
  bio: string;            // 个人陈述
  careerGoal: string;     // 职业目标
  email: string;          // 邮箱
  linkedin: string;       // LinkedIn 链接
}
```

### projects.ts
```typescript
interface Project {
  id: string;             // 唯一标识
  name: string;           // 项目名称
  summary: string;        // 一句话概述
  metrics: {              // 3个关键指标
    label: string;
    value: string;        // 如 "+35%"
    description: string;
  }[];
  background: string;     // 背景与挑战
  myRole: string;         // 我的角色
  solution: string;       // 方案与决策
  beforeAfter: {          // 对比数据
    label: string;
    before: string;
    after: string;
  }[];
  chartImage: string;     // Power BI 导出的图表图片路径
  reflection: string;     // 复盘与反思
}

type ProjectsData = Project[];  // 2-3个
```

### articles.ts
```typescript
interface Article {
  title: string;          // 标题
  date: string;           // 发布日期
  summary: string;        // 摘要
  url: string;            // 外部链接
}

type ArticlesData = Article[];   // 0-2篇
```

---

## 五、部署架构

```
┌──────────────┐     git push      ┌──────────────┐
│   本地开发    │ ─────────────────→ │   GitHub     │
│  (localhost) │                    │   Repository │
└──────────────┘                    └──────┬───────┘
                                           │
                                    GitHub Actions
                                    (deploy.yml)
                                           │
                                           ▼
                                    ┌──────────────┐
                                    │  gh-pages    │
                                    │  branch      │
                                    └──────┬───────┘
                                           │
                                    GitHub Pages
                                           │
                                           ▼
                              ┌──────────────────────┐
                              │  3062538987.github   │
                              │  .io/product-        │
                              │  portfolio           │
                              └──────────────────────┘
```

### GitHub Actions 流程

```yaml
# .github/workflows/deploy.yml
on:
  push:
    branches: [master]

jobs:
  deploy:
    steps:
      1. Checkout 代码
      2. 安装 Node.js 18
      3. npm install
      4. npm run build (Vite 构建 → dist/)
      5. 部署到 gh-pages 分支 (peaceiris/actions-gh-pages)
```

---

## 六、技术层级依赖关系

```
Browser (Chrome/Edge/Safari)
    │
    ▼
React 18 (UI 框架)
    │
    ├── lucide-react (图标)
    ├── react-to-print (PDF 导出)
    └── Tailwind CSS 3 (样式)
    │
    ▼
TypeScript 5 (类型检查)
    │
    ▼
Vite 5 (构建工具)
    │
    ▼
Node.js 18+ (运行时)
    │
    ▼
GitHub Pages (托管)
```

---

> 待讨论确认：组件树是否完整？数据模型字段是否够用？部署架构是否清晰？